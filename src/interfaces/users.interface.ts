export interface IUser {
    firstName: string;
    lastName: string;
    email: string;
    picture: string;
    password: string;
    address: string;
    activated: boolean;
    levelAuthority: string;
}